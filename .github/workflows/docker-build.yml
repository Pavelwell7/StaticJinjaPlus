name: Build and Push Docker Images

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push latest
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: pavelwell/static-jinja-plus:latest
          build-args: SJP_VERSION=0.1.1

      - name: Build and push 0.1.1
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: pavelwell/static-jinja-plus:0.1.1
          build-args: SJP_VERSION=0.1.1

      - name: Build and push 0.1.0
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: pavelwell/static-jinja-plus:0.1.0
          build-args: SJP_VERSION=0.1.0

      - name: Build and push develop
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: pavelwell/static-jinja-plus:develop
          build-args: SJP_VERSION=main
